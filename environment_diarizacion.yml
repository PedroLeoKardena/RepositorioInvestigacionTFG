name: env_diarizacion
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - ffmpeg
  - pip
  - pip:
      - pyannote.audio
      - torch
      - torchaudio
      - librosa
      - soundfile
      - huggingface_hub
